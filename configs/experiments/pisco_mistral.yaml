dataset:
  class: projected_token.datasets.PopqaDataset

model:
  class: projected_token.models.PiscoModel
  kwargs:
    model_name_or_path: "naver/pisco-mistral"
    device: "cuda:0"
    trust_remote_code: true

generation:
  max_new_tokens: 512

experiment:
  sample_count: 1
